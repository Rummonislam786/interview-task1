import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/context/AuthContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Your Store | ExpressIt BD",
  description: "Check domain availability and create your online store easily with ExpressIt BD.",
  keywords: ["domain availability", "store creation", "ExpressIt BD", "Next.js"],
  openGraph: {
    title: "Create Your Store | ExpressIt BD",
    description: "Check domain availability and create your online store easily with ExpressIt BD.",
    url: "https://expressitbd.com/",
    siteName: "ExpressIt BD",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
